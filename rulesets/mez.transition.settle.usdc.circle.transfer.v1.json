{
  "type": "MEZTransitionRuleset",
  "ruleset_id": "mez.transition.settle.usdc.circle.transfer.v1",
  "version": "0.1.0",
  "transition_kind": "settle.usdc.circle.transfer.v1",
  "payload_schema": "schemas/transition.payload.settle.usdc.circle.transfer.v1.schema.json",
  "validation": {
    "must_validate_payload_against_schema": true,
    "must_reject_float_numbers_in_payload": true,
    "notes": [
      "Amounts should be encoded as decimal strings.",
      "This ruleset is a stub; production deployments SHOULD supplement with provider-specific invariants (chain allowlists, wallet allowlists, transfer_id uniqueness, rate limits)."
    ]
  }
}
