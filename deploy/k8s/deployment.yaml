# MEZ API Server — Kubernetes Deployment
#
# Runs the Rust mez-api binary as a Deployment with:
# - Rolling update strategy (zero-downtime deploys)
# - Resource limits appropriate for a stateless API server
# - Liveness and readiness probes against dedicated health endpoints
# - Security context: non-root, read-only filesystem, no privilege escalation
#
# Apply: kubectl apply -f deploy/k8s/

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mez-api
  namespace: mez
  labels:
    app: mez-api
    component: api-server
    part-of: mez-stack
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: mez-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: mez-api
        component: api-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: mez-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      terminationGracePeriodSeconds: 30
      containers:
        - name: mez-api
          image: ghcr.io/momentum-ez/mez-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: MEZ_HOST
              value: "0.0.0.0"
            - name: MEZ_PORT
              value: "8080"
            - name: MEZ_LOG_LEVEL
              value: "info"
            - name: MEZ_METRICS_ENABLED
              value: "true"
            # Zone configuration from ConfigMap
            - name: MEZ_ZONE_ID
              valueFrom:
                configMapKeyRef:
                  name: mez-config
                  key: zone-id
            - name: MEZ_JURISDICTION
              valueFrom:
                configMapKeyRef:
                  name: mez-config
                  key: jurisdiction
            - name: MEZ_PROFILE
              valueFrom:
                configMapKeyRef:
                  name: mez-config
                  key: profile
            # Database credentials from Secret (managed by ExternalSecrets in prod)
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: mez-secrets
                  key: database-url
            # JWT signing key from Secret
            - name: MEZ_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: mez-secrets
                  key: jwt-secret
            # Zone signing key (managed by ExternalSecrets in prod)
            - name: ZONE_SIGNING_KEY_HEX
              valueFrom:
                secretKeyRef:
                  name: mez-signing-key
                  key: signing-key-hex
                  optional: true
            # Key provider backend (software | aws-kms | aws-cloudhsm)
            - name: MEZ_KEY_PROVIDER
              valueFrom:
                configMapKeyRef:
                  name: mez-config
                  key: key-provider
                  optional: true
            # Mass API credentials (managed by ExternalSecrets in prod)
            - name: MASS_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mez-mass-credentials
                  key: mass-api-token
                  optional: true
            - name: AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mez-mass-credentials
                  key: auth-token
                  optional: true
          # Health probes
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 2
          startupProbe:
            httpGet:
              path: /health/liveness
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          # Resource limits
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          # Security
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          # Writable tmp for Axum/Tokio
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: app-data
              mountPath: /app/data
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
        - name: app-data
          emptyDir:
            sizeLimit: 256Mi
