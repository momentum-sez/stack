# MEZ Stack — Kubernetes Namespace + ServiceAccount
#
# Apply first: kubectl apply -f deploy/k8s/namespace.yaml

apiVersion: v1
kind: Namespace
metadata:
  name: mez
  labels:
    app: mez-stack

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: mez-api
  namespace: mez
  labels:
    app: mez-api
