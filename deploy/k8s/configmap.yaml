# MEZ API Server — Runtime Configuration
#
# Non-sensitive configuration values. For secrets (database credentials,
# JWT signing keys), see secret.yaml.

apiVersion: v1
kind: ConfigMap
metadata:
  name: mez-config
  namespace: mez
  labels:
    app: mez-api
    component: config
data:
  # Zone identity
  zone-id: "org.momentum.mez.zone.production"
  jurisdiction: "pk"
  profile: "digital-financial-center"
  # Corridor defaults
  corridor-id: "org.momentum.mez.corridor.pk-ae"
  # Watcher configuration
  watcher-id: "watcher-pk-001"
  bond-amount: "100000"
