# A/B testing framework

This module defines an experiment descriptor schema so zones can conduct staged rollouts with auditability.

Experiments that change policy/legal artifacts should be expressed through overlays and pinned in lockfiles.
