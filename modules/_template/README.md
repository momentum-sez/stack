# Module Template

Copy this directory to create a new module.

- `module.yaml` — manifest
- `src/` — source artifacts (legal texts, policies, schemas)
- `tests/` — conformance tests
- `docs/` — module documentation
