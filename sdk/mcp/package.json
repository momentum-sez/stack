{
  "name": "@mass/stack-mcp",
  "version": "0.1.0",
  "description": "MCP server for Mass zone deployment - agent-native zone bootstrap from the open-source template",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "stack-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "yaml": "^2.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
