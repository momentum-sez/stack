# Metrics overview

Metrics SHOULD be:

- named (stable identifiers)
- versioned (schema evolution)
- auditable (measurement method recorded)

This module includes a small starter registry in `registry/metrics.yaml`.
