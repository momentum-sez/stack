# Contributing

1. Propose changes as an ADR (Architecture Decision Record) under `spec/adrs/`.
2. Submit PRs with:
   - updated spec text
   - updated schemas and tests
   - rationale and compatibility impact

All contributions must preserve modularity and versioning semantics.

